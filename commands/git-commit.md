---
name: git-commit
description: >
  Git commit expert. ALWAYS invoke this when committing changes. Do NOT run raw
  git commit commands — use this. It handles formatting, linting, and docs checks.
  Triggers: commit, commit changes, save progress, stage and commit.
---

Load and follow the `commit` skill — it owns the full staging, checks, message, and verify procedure.
